# Khan v1.0.8 — Plan (Planned)

> **وضعیت:** تعریف شده — اجرا نشده
> **تاریخ:** 2026-08-20
> **تعداد موارد:** 35
> **لوگو:** `logo-khan.jpg` (لوگوی رسمی خان)
> **طراحی UI:** `design/khan-design-v1.0.8.html` (Blueprints)

---

## 🔴 مرحله ۱ — Critical (امنیت + کشنده) → ۷ مورد

### 1. Setup race condition
- **وضعیت فعلی:** NeedsSetup + Create admin یکجا atomic نیستن — دوتا ادمین هم‌زمان میتونن ساخته بشن
- **فیکس:** Mutex در NeedsSetup → Setup (یا فقط Setup با lock) + بررسی دوباره قبل از ساخت

### 2. Settings بدون نقش ادمین
- **وضعیت فعلی:** `/api/settings/license`، `/api/settings/network`، `/api/settings/backup`، `/api/users/export` فقط `RequireAuth` دارن — نه `requireRole`
- **فیکس:** گروه route با `requireRole("admin")` یا `requireRole("supervisor")` برای هرکدوم مناسبه

### 3. Stored XSS در لینکهای پیام
- **وضعیت فعلی:** ریجکس `/(https?:\/\/[^\s<]+)/g)` — کاراکتر `"` و `'` فیلتر نمیشه
- **پروداکشن:** `https://evil.com/" onmouseover="alert(1)` → attribute injection
- **فیکس:** escape کردن `"` و `'` قبل از regex + اضافه کردن `[^"'\s]` + href validation

### 4. WS CheckOrigin = true
- **وضعیت فعلی:** `CheckOrigin: func(r) bool { return true }`
- **فیکس:** allowlist: localhost, 192.168.x.x, 10.x.x.x, 172.16-31.x.x + env var اختیاری

### 5. توکن در URL
- **وضعیت فعلی:** WS `/ws?token=` + File download `?token=`
- **فیکس:** WS → `Sec-WebSocket-Protocol` header یا cookie؛ File → Authorization header

### 6. GenerateKey panic
- **وضعیت فعلی:** `rand.Read` خطا → panic → سرور کل میمیره
- **فیکس:** error propagation + fallback به `crypto/rand` اگه مشکلی پیش اومد

### 7. سشن‌های مرده جمع میشن
- **وضعیت فعلی:** `CleanupExpired()` تعریف شده ولی هیچجا صدا زده نمیشه
- **فیکس:** اجرای periodic cleanup (مثلاً هر ۱ ساعت یا هر startup)

---

## 🟠 مرحله ۲ — Important (عملکردی + امنیتی) → ۱۱ مورد

### 8. Rate limit روی دیسک
- **وضعیت فعلی:** ۵ تلاش/۵ دقیقه فقط in-memory — ری‌استارت پاک میشه
- **فیکس:** ذخیره شمارنده + timestamp در فایل JSON (مثلاً `.login-attempts.json`)

### 9. PollVote بدون چک عضویت
- **وضعیت فعلی:** `PollRepo.Vote()` — هر کسی poll_id داشته باشه رای میده
- **فیکس:** اضافه کردن `IsMember(poll.RoomID, userID)` در Vote()

### 10. PollClose بدون چک نقش
- **وضعیت فعلی:** هر کسی میتونه نظرسنجی رو ببنده
- **فیکس:** فقط مالک پیام یا admin/supervisor بتونه ببنده

### 11. Avatar upload: route نداره
- **وضعیت فعلی:** JS `POST /api/users/avatar` → SPA fallback (200+HTML)
- **فیکس:** ساخت handler در `user_handler.go` + upload به disk + ذخیره مسیر در User model

### 12. Profile save: route نداره
- **وضعیت فعلی:** JS `PUT /api/users/me` → SPA fallback — دکمه ذخیره مرده
- **فیکس:** ساخت handler در `user_handler.go` + آپدیت display_name

### 13. جستجو limit ۳۰ ثابت
- **وضعیت فعلی:** `limit: 30` بدون صفحه‌بندی — نتایج قدیمی‌تر پیدا نمیشه
- **فیکس:** اضافه کردن `offset` parameter + صفحه‌بندی در frontend

### 14. محدودیت طول پیام نیست
- **وضعیت فعلی:** مثل فایلها `MaxUploadMB` هست ولی متن پیام max نداره
- **فیکس:** اضافه کردن `MaxMessageChars` در config (مثلاً ۱۰,۰۰۰ کاراکتر)

### 15. تغییر رمز → سشن‌های قدیمی معتبر
- **وضعیت فعلی:** کاربر رمز عوض کنه، دستگاه‌های دیگه هنوز login هستن
- **فیکس:** حذف همه سشن‌های کاربر بعد از ChangePassword

### 16. hidden admin لو میره
- **وضعیت فعلی:** `GET /api/users` فلگ `hidden: true` برای aDiB برمیگردونه
- **فیکس:** فیلتر کردن hidden users از response (نه فقط ListVisible، بلکه همه جا)

### 17. رمزنگاری end-to-end
- **وضعیت فعلی:** باید چک بشه همه handler ها درست encrypt/decrypt کنن
- **فیکس:** بررسی + تست: هیچ handler ای متن ساده نباید بنویسه

### 18. JSON store همزمانی
- **وضعیت فعلی:** RWMutex سراسری هست ولی بعضی جاها lock ندارن
- **فیکس:** بررسی همه repository ها — همه write operations باید mutex داشته باشن

---

## 🟡 مرحله ۳ — Medium (UX + عملکردی) → ۹ مورد

### 19. پنل ادمین جدا (`/panel`)
- **وضعیت فعلی:** `/api/setup` با هر URL قابل دسترسیه
- **فیکس:** آدرس جداگانه `/panel` برای setup اولیه — هرکسی با اولین ورود نتونه ادمین بسازه

### 20. Validation طول Room Name
- **وضعیت فعلی:** فقط `name == ""` — max length نیست
- **فیکس:** max 100 کاراکتر + filter XSS در نام

### 21. Validation طول Poll Option
- **وضعیت فعلی:** min 2, max 10 گزینه — ولی هر گزینه بدون max length
- **فیکس:** max 200 کاراکتر برای هر گزینه

### 22. خطاهای فارسی توی لاگ
- **وضعیت فعلی:** `writeErr` پیام فارسی — monitoring سخته
- **فیکس:** انگلیسی توی لاگ + فارسی فقط به کلاینت

### 23. Push notifications
- **وضعیت فعلی:** نیست
- **فیکس:** Service Worker + نوتیفیکیشن مرورگر (Web Notification API)

### 24. Thread / Reply UI درختی
- **وضعیت فعلی:** ریپلای هست ولی نمایش نخ گفتگو نه
- **فیکس:** UI درختی برای reply chains (مثل تلگرام)

### 25. کیبورد شورتکات
- **وضعیت فعلی:** نیست
- **فیکس:** Cmd+K جستجو، Esc بستن، Enter ارسال

### 26. File download token در URL
- **وضعیت فعلی:** `/api/files/{id}/download?token=`
- **فیکس:** Authorization header (مثل بقیه routeها)

### 27. Channel description/topic validation
- **وضعیت فعلی:** فیلدهای اضافی بدون max length
- **فیکس:** max 500 کاراکتر برای description، max 200 برای topic

---

## 🟢 مرحله ۴ — Nice-to-have (UI + زیرساخت) → ۸ مورد

### 28. Virtual scrolling + lazy load عکس
- **وضعیت فعلی:** همه پیام‌ها یکجا load میشن
- **فیکس:** Virtual scrolling برای پیام‌ها + lazy load برای تصاویر

### 29. دسترس‌پذیری ARIA + focus
- **وضعیت فعلی:** ARIA کمتر
- **فیکس:** aria-label روی همه دکمه‌ها، focus trap در modalها، screen reader support

### 30. E2E تست Playwright + CI/CD
- **وضعیت فعلی:** تست دستی
- **فیکس:** Playwright test suite + GitHub Actions CI

### 31. CSS minify
- **وضعیت فعلی:** ۳۳۵KB
- **فیکس:** Minify به ~۲۰۰KB (postcss + cssnano)

### 32. Logout → پاک کردن localStorage
- **وضعیت فعلی:** باید بررسی بشه
- **فیکس:** حذف کامل `khan_theme` و بقیه کلیدها

### 33. سیستم ۱۲ تم جدید
- **وضعیت فعلی:** ۲ تم (dark/light)
- **فیکس:** ۶ پایه (خان/یاقوت/بهار/شب/غروب/برف) × روشن/تیره + CSS variables + color-mix

### 34. صفحه لاگین بازطراحی
- **وضعیت فعلی:** ساده
- **فیکس:** پنل برندینگ + تایلهای شناور + سوییچ تم/زبان (از khan-design-v1.0.8.html)

### 35. فونت Vazirmatn split
- **وضعیت فعلی:** base64 امبد در CSS (~۲۰۰KB)
- **فیکس:** جدا کردن به فایل `.woff2` + `@font-face` جداگانه

---

## 🗓️ ترتیب اجرا + بکاپ

| مرحله | موارد | بکاپ |
|-------|-------|------|
| ۱ — Critical | ۱-۷ | ✅ `commit: security-fixes` |
| ۲ — Important | ۸-۱۸ | ✅ `commit: functional-fixes` |
| ۳ — Medium | ۱۹-۲۷ | ✅ `commit: ux-improvements` |
| ۴ — Nice-to-have | ۲۸-۳۵ | ✅ `commit: polish-and-ui` |
| نهایی | build + test + release | ✅ `commit: v1.0.8-release` |

---

## 📊 خلاصه اعداد

| دسته | تعداد | شدت |
|------|-------|------|
| 🔴 Critical | ۷ | فوری |
| 🟠 Important | ۱۱ | قبل از انتشار |
| 🟡 Medium | ۹ | در حین توسعه |
| 🟢 Nice-to-have | ۸ | اختیاری |
| **جمع** | **۳۵** | |

---

## 📝 یادداشت‌ها

- **لوگو:** `logo-khan.jpg` — متن طلایی «خان» + سقف خانه + سبیل طلایی + حباب چت
- **طراحی UI:** `design/khan-design-v1.0.8.html` — Blueprints برای ۱۲ تم جدید + صفحه لاگین + دراور
- **لوگو:** ذخیره شده در `logo-khan.jpg`
- **وضعیت:** این سند فقط plan هست — اجرا نشده
